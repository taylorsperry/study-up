import React from 'react';
import './scss/_Header.scss';

const Header = props => {
    return (
        <section className="header">
            <h1 className="logo">StudyUp</h1>
        </section>
    );
};

export default Header;